import clsx from 'clsx';

export default function Footer() {
	return (
		<footer
			className={clsx('bg-primary', 'text-primary-foreground', 'p-4', "text-center")}
		>
			spisenkelt.no
		</footer>
	);
}
